# Curiosity

## Deployment guide

First, you need to install all dependencies.
At your project root directory, tape in your terminal :

```sh
npm i --save
```

Then, run the server :

```sh
node app.js
```

That's it !